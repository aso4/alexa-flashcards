<!doctype html>

<html lang="en">
<head>
  <meta charset="utf-8">
  <%= favicon_link_tag 'favicon.ico' %>
  <title>Ruby Flashcards for Alexa</title>
  <meta name="description" content="Ruby Flashcards for Amazon's Alexa Skills">

</head>

<body>
  Hello and welcome to Ruby Flashcards for Alexa!

  <ul>
    <% @account_summaries.items.each do |item| %>
      <li><%= item.name %> (<%= item.id %>)
        <ul>
          <% item.web_properties.each do |property| %>
            <li><%= property.name %> (<%= property.id %>)
              <ul>
                <% property.profiles.each do |profile| %>
                  <li><%= profile.name %> (<%= profile.id %>)</li>
                <% end %>
              </ul>
            </li>
          <% end %>
        </ul>
      </li>
    <% end %>
  </ul>  
</body>
</html>
